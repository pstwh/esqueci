/**
     * Lista os registros.
     *
     * @return \Illuminate\Http\Response
     */
    public function index()
    {
        $#{pluralVariable} = #{modelName}::paginate(10);

        return view('#{namespace}.#{modelName}.index', compact('#{pluralVariable}'));
    }